<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>oldgame.play</title>
<meta name="viewport" content="width=device-width, initial-scale=1">

<!-- SEO -->
<meta name="description" content="oldgame.play is a retro-inspired browser gaming site with fully original, online-only mini games. No downloads, no emulators, just instant play.">
<meta name="robots" content="index, follow">

<!-- OPEN LICENSE FONTS -->
<link href="https://fonts.googleapis.com/css2?family=VT323&family=Share+Tech+Mono&display=swap" rel="stylesheet">

<style>
:root{
  --bg-main:#020617;
  --panel:#0f172a;
  --frame:#111827;
  --screen:#1f2933;
  --text-main:#e5e7eb;
  --text-soft:#9ca3af;
  --accent:#38bdf8;
}

*{margin:0;padding:0;box-sizing:border-box}

body{
  background:var(--bg-main);
  color:var(--text-main);
  font-family:'VT323','Share Tech Mono',monospace;
  min-height:100vh;
}

/* CRT EFFECT */
body::before{
  content:"";
  position:fixed;
  inset:0;
  background:repeating-linear-gradient(
    180deg,
    rgba(255,255,255,.02) 0px,
    rgba(255,255,255,.02) 1px,
    transparent 2px,
    transparent 4px
  );
  pointer-events:none;
}
body::after{
  content:"";
  position:fixed;
  inset:0;
  box-shadow:inset 0 0 120px rgba(0,0,0,.9);
  pointer-events:none;
}

header{
  text-align:center;
  padding:20px;
}
header h1{font-size:36px;letter-spacing:2px}

.phone-frame{
  width:360px;
  max-width:95%;
  margin:20px auto;
  aspect-ratio:9/16;
  background:var(--frame);
  border-radius:20px;
  padding:14px;
}

.phone-screen{
  background:var(--screen);
  height:100%;
  border-radius:14px;
  padding:12px;
  display:flex;
  flex-direction:column;
}

/* BUTTONS */
button{
  background:var(--panel);
  color:var(--text-main);
  border:none;
  padding:14px;
  margin-bottom:10px;
  border-radius:12px;
  font-size:20px;
  cursor:pointer;
}
button:active{
  transform:scale(.96);
  box-shadow:0 0 10px var(--accent);
}

/* CANVAS */
canvas{
  background:#020617;
  border-radius:10px;
  display:block;
  margin:auto;
  touch-action:manipulation;
}

footer{
  text-align:center;
  font-size:14px;
  color:var(--text-soft);
  padding:20px;
}
</style>
</head>

<body>

<header>
  <h1>oldgame.play</h1>
  <p>original browser retro games</p>
</header>

<div class="phone-frame">
  <div class="phone-screen">

    <!-- HOME -->
    <div id="home">
      <button onclick="openGame()">PLAY GAME</button>
      <button onclick="openSettings()">SETTINGS</button>
    </div>

    <!-- GAME -->
    <div id="game" style="display:none">
      <canvas id="gameCanvas" width="300" height="520"></canvas>
      <button onclick="goHome()">BACK</button>
    </div>

    <!-- SETTINGS -->
    <div id="settings" style="display:none">
      <button onclick="toggleSound()">SOUND: <span id="soundState">OFF</span></button>
      <button onclick="goHome()">BACK</button>
    </div>

  </div>
</div>

<footer>
<p>
All games on oldgame.play are fully original, browser-based experiences.
Online-play only. No downloads, ROMs, emulators, or brand assets.
</p>
</footer>

<script>
/* NAVIGATION */
const home=document.getElementById("home");
const game=document.getElementById("game");
const settings=document.getElementById("settings");

function goHome(){
  home.style.display="block";
  game.style.display="none";
  settings.style.display="none";
}
function openGame(){
  home.style.display="none";
  game.style.display="block";
  settings.style.display="none";
  startGame();
}
function openSettings(){
  home.style.display="none";
  game.style.display="none";
  settings.style.display="block";
}

/* SOUND */
let soundEnabled=false;
let audioCtx;

function toggleSound(){
  soundEnabled=!soundEnabled;
  document.getElementById("soundState").textContent=soundEnabled?"ON":"OFF";
}

function beep(freq=700,time=0.05){
  if(!soundEnabled) return;
  if(!audioCtx){
    audioCtx=new (window.AudioContext||window.webkitAudioContext)();
  }
  const osc=audioCtx.createOscillator();
  osc.frequency.value=freq;
  osc.connect(audioCtx.destination);
  osc.start();
  osc.stop(audioCtx.currentTime+time);
}

/* GAME: ORB SHIFT (ORIGINAL) */
const canvas=document.getElementById("gameCanvas");
const ctx=canvas.getContext("2d");
let orb,dir,anim;

function startGame(){
  orb={x:150,y:260};
  dir=1;
  cancelAnimationFrame(anim);
  loop();
}

function loop(){
  ctx.clearRect(0,0,canvas.width,canvas.height);

  ctx.fillStyle="#38bdf8";
  ctx.beginPath();
  ctx.arc(orb.x,orb.y,12,0,Math.PI*2);
  ctx.fill();

  orb.y+=2*dir;
  if(orb.y<40||orb.y>480){
    dir*=-1;
    beep();
  }

  anim=requestAnimationFrame(loop);
}

/* INPUT */
window.addEventListener("keydown",e=>{
  if(e.key===" "){
    dir*=-1;
    beep(900);
  }
});

/* TOUCH CONTROL */
canvas.addEventListener("touchstart",()=>{
  dir*=-1;
  beep(900);
});
</script>

</body>
</html>
